import { ModuleScreen } from '@/src/features/shell/ModuleScreen';

export default function StashScreen() {
  return (
    <ModuleScreen
      title="Stash"
      description="Wishlist and saved finds for your household. This module stays an empty shell until after Meals MVP."
    />
  );
}
